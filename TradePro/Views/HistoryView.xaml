<UserControl x:Class="TradePro.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0E0F10">
        <StackPanel Margin="24">
            <TextBlock Text="Historial de trades" Foreground="White" FontSize="22" FontWeight="Bold" />
            <TextBlock Text="Registros de operaciones cerradas y abiertas" Foreground="#BDBDBD" Margin="0,6,0,12" />

            <Border Background="#0F1313" CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="Operaciones recientes" Foreground="#BDBDBD" Margin="0,0,0,8" />
                    <ListBox x:Name="TradesList" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="HorizontalAlignment" Value="Stretch" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>