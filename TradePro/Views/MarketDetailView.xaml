<UserControl x:Class="TradePro.Views.MarketDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <!-- Theme brushes -->
        <SolidColorBrush x:Key="BackgroundDark" Color="#0E0F10" />
        <SolidColorBrush x:Key="PanelBg" Color="#0F1313" />
        <SolidColorBrush x:Key="CardBg" Color="#0F1313" />
        <SolidColorBrush x:Key="MutedText" Color="#9A9A9A" />
        <SolidColorBrush x:Key="AccentBlue" Color="#2D8CFF" />
        <SolidColorBrush x:Key="LongGreen" Color="#1E7F3E" />
        <SolidColorBrush x:Key="ShortRed" Color="#B71C1C" />

        <!-- Modern rounded button style with subtle shadow and scale on hover -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Background" Value="{StaticResource CardBg}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,2" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.98" />
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.02" ScaleY="1.02" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.9" />
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small flat preset button -->
        <Style x:Key="PresetButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#15181A" />
            <Setter Property="Width" Value="56" />
            <Setter Property="Height" Value="28" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="6,2" />
        </Style>

        <!-- Slider thumb style simplified -->
        <Style TargetType="Slider" x:Key="ModernSlider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Track x:Name="PART_Track"  VerticalAlignment="Center">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{x:Null}" IsTabStop="False" Opacity="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{x:Null}" IsTabStop="False" Opacity="0" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="14" Height="14">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="White" Stroke="#444" Width="14" Height="14" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundDark}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left: trading panel (card) -->
        <Border Grid.Column="0" Background="{StaticResource PanelBg}" CornerRadius="12" Padding="16" Margin="20">
            <StackPanel>
                <!-- Header with symbol and price (subtle gradient) -->
                <Border CornerRadius="10" Padding="12" Margin="0,0,0,12">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#0F1316" Offset="0" />
                            <GradientStop Color="#101418" Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="SymbolText" Text="SYMBOL" Foreground="White" FontSize="26" FontWeight="Bold" />
                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0" VerticalAlignment="Center">
                                <TextBlock x:Name="PriceText" Text="$0.00" Foreground="{StaticResource MutedText}" FontSize="13" />
                                <TextBlock x:Name="PriceChangeIcon" Text="▲" Foreground="#4CAF50" FontSize="13" Margin="8,0,0,0" Visibility="Collapsed" />
                                <TextBlock x:Name="PriceChangeText" Text="0.00%" Foreground="#4CAF50" FontSize="12" Margin="6,0,0,0" Visibility="Collapsed" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Side selector -->
                <TextBlock Text="Side" Foreground="{StaticResource MutedText}" Margin="2,26,0,6" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <Button x:Name="LongButton" Content="LONG" Click="LongButton_Click" Style="{StaticResource ModernButton}" Background="{StaticResource LongGreen}" Width="96" Height="40" Margin="0,0,8,0" ToolTip="Abrir posicion larga" />
                    <Button x:Name="ShortButton" Content="SHORT" Click="ShortButton_Click" Style="{StaticResource ModernButton}" Background="{StaticResource ShortRed}" Width="96" Height="40" ToolTip="Abrir posicion corta" />
                </StackPanel>

                <!-- Leverage -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,10,0,22">
                    <StackPanel Width="260">
                        <TextBlock Text="Leverage" Foreground="{StaticResource MutedText}" />
                        <Slider x:Name="LeverageSlider" Style="{StaticResource ModernSlider}" Minimum="1" Maximum="100" Value="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="LeverageSlider_ValueChanged" Margin="0,8,0,0" />
                    </StackPanel>
                    <Border Background="#141618" CornerRadius="8" Padding="8" Margin="12,0,0,0" Width="72" Height="48" VerticalAlignment="Center">
                        <TextBlock x:Name="LeverageText" Text="1x" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </StackPanel>

                <!-- Amount -->
                <TextBlock Text="Cantidad ($)" Foreground="{StaticResource MutedText}" />
                <StackPanel Margin="0,6,0,18">
                    <Slider x:Name="AmountSlider" Style="{StaticResource ModernSlider}" Minimum="0" Maximum="1000" Value="100" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="AmountSlider_ValueChanged" />
					<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
						<Button Content="25%" Style="{StaticResource PresetButton}" Margin="8,0,0,0" Click="Preset25_Click" />
						<Button Content="50%" Style="{StaticResource PresetButton}" Margin="8,0,0,0" Click="Preset50_Click" />
						<Button Content="75%" Style="{StaticResource PresetButton}" Margin="8,0,0,0" Click="Preset75_Click" />
						<Button Content="100%" Style="{StaticResource PresetButton}" Margin="8,0,0,0" Click="Preset100_Click" />
					</StackPanel>
					<StackPanel Orientation="Horizontal" Margin="0,15,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="AmountText" Text="$100.00" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock x:Name="BalanceText" Text="(Balance: $0.00)" Foreground="{StaticResource MutedText}" Margin="12,0,0,0" VerticalAlignment="Center" />
                        
                    </StackPanel>
                </StackPanel>

                <!-- Open button and status -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,25,0,0">
                    <Button x:Name="OpenButton" Content="Abrir posicion" Width="200" Height="54" Click="OpenButton_Click" Style="{StaticResource ModernButton}" Background="#1A2121" />
                    <TextBlock x:Name="OrderStatusText" Text="" Foreground="#FFB86B" Margin="12,12,0,0" VerticalAlignment="Center" />
                </StackPanel>

            </StackPanel>
        </Border>

        <!-- Right: chart area -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,12">
                <Button x:Name="Btn1H" Content="1H" Width="48" Margin="0,0,8,0" Click="Btn1H_Click" Style="{StaticResource ModernButton}" Background="#1A2121"/>
                <Button x:Name="Btn4H" Content="4H" Width="48" Margin="0,0,8,0" Click="Btn4H_Click" Style="{StaticResource ModernButton}" Background="#1A2121"/>
                <Button x:Name="Btn1D" Content="1D" Width="48" Margin="0,0,8,0" Click="Btn1D_Click" Style="{StaticResource ModernButton}" Background="#1A2121"/>
            </StackPanel>

            <Border Grid.Row="1" Background="{StaticResource CardBg}" CornerRadius="12" Padding="12">
                <oxy:PlotView x:Name="ChartPlotView" Height="820" Background="#0E0F10" />
            </Border>
        </Grid>
    </Grid>
</UserControl>