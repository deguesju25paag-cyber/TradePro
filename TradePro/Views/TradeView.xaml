<UserControl x:Class="TradePro.Views.TradeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0E0F10">
        <StackPanel Margin="20">
            <TextBlock Text="Trade View" Foreground="White" FontSize="20" FontWeight="SemiBold" />
            <!-- Placeholder for chart and order form -->
            <Border Background="#0F1313" CornerRadius="8" Padding="12" Margin="0,12,0,0">
                <TextBlock Text="Chart area + order form (to implement)" Foreground="#BDBDBD" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>