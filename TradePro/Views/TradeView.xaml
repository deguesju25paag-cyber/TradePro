<UserControl x:Class="TradePro.Views.TradeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0E0F10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="20" Grid.Row="0">
            <TextBlock Text="Trade - Markets" Foreground="White" FontSize="20" FontWeight="SemiBold" />
        </StackPanel>

        <Border Grid.Row="1" Background="#0F1313" CornerRadius="8" Padding="12" Margin="20,0,20,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="Listado de activos" Foreground="#BDBDBD" />

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
                    <ItemsControl x:Name="AssetsList">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.Resources>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="HorizontalAlignment" Value="Stretch" />
                            </Style>
                        </ItemsControl.Resources>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Pagination controls -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,20">
            <Button x:Name="BtnPrev" Content="Anterior" Width="90" Margin="6" />
            <TextBlock x:Name="PageIndicator" Text="Pagina 1/1" Foreground="#BDBDBD" VerticalAlignment="Center" Margin="6" />
            <Button x:Name="BtnNext" Content="Siguiente" Width="90" Margin="6" />
        </StackPanel>
    </Grid>
</UserControl>
